ClientCucumberRunner (client-tests)
        ↓
extends BaseAutwitCucumberRunner (autwit-runner)
        ↓
Spring Boot Context starts (runner)
        ↓
AutwitProfileInitializer runs
        ↓
application.yaml (runner) loaded FIRST
        ↓
application-test.yml (client) merged
